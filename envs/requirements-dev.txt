# Development + tests + notebook file validation (nbformat).
#   pip install -r requirements-dev.txt
#   pip install -e .

-r requirements.txt
pytest>=7.4
pytest-cov>=4.1
nbmake>=1.5
nbformat>=5.10
python-dotenv>=1.0
