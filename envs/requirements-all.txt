# Full workspace: dev tooling + notebooks + local inference.
#   pip install -r requirements-all.txt
#   pip install -e .

-r requirements-dev.txt
-r requirements-notebooks.txt
-r requirements-local.txt
