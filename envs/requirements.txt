# Minimal install: remote HF Inference + YAML scenarios + simulation core.
# Install from repo root (editable, picks up src/):
#   pip install -r requirements.txt
# Or use pyproject (recommended): pip install -e .

pydantic>=2.5
PyYAML>=6.0
Jinja2>=3.1
huggingface_hub>=0.22
